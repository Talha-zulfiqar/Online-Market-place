# Online Market Place

Simple C++ Visual Studio project for an online marketplace.

Build (Visual Studio):
- Open `Online Market Pl ace.vcxproj` in Visual Studio and build for Debug x64.

Notes:
- This repository contains build artifacts in `x64/` which are ignored by `.gitignore`.
